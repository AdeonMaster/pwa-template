// import { askUserForNotificationPermissions, sendPushNotification, isPushNotificationSupported } from '../push-notification';

describe('push-notification utils', () => {
  describe('askUserForNotificationPermissions', () => {
    it.todo('should resolves to default');
  });
});
